baseURL = 'https://wanderingmind.com/'
languageCode = 'en-us'
title = 'The Wandering Mind'
theme = 'hugo-theme-stack'
copyright = 'Devanshi. All Rights Reserved'

# -----------------------------------------------------------
# GENERAL SETTINGS
# -----------------------------------------------------------
[params]
    mainSections = ['post']
    featuredImageField = 'image'
    rssFullContent = true
    
    [params.favicon]
    
    # Color scheme (Auto switches between dark/light mode)
    [params.colorScheme]
        toggle = true
        default = "auto"
    
    # The Sidebar (Left side)
    [params.sidebar]
        emoji = "📚"
        subtitle = "Exploring History, Mathematics, Space & Nature"
        
        [params.sidebar.avatar]
            enabled = true
            local = true
            src = "img/avatar.jpg"  # Ensure file is at: assets/img/avatar.jpg
    
    # Article Settings (Essential for your Math posts)
    [params.article]
        math = true          # Enables Math formulas like LaTeX
        toc = true           # Enables Table of Contents
        readingTime = true
    
        [params.article.license]
            enabled = false
    
    # Comments (Disabled)
    [params.comments]
        enabled = false
    
    # Widgets configuration (The Right Sidebar / Mobile Menu)
    [params.widgets]
        # FIX: We use { type = "..." } instead of just "..."
        homepage = [
            { type = "search" },
            { type = "archives" },
            { type = "categories" },
            { type = "tag-cloud" }
        ]
        
        # FIX: Do the same for the page widgets
        page = [
            { type = "toc" }
        ]

    # Footer
    [params.footer]
        since = 2025

# -----------------------------------------------------------
# NAVIGATION MENU
# -----------------------------------------------------------
[[menu.main]]
    identifier = "home"
    name = "Home"
    url = "/"
    weight = 1
    [menu.main.params]
        icon = "home"

[[menu.main]]
    identifier = "archives"
    name = "All Posts"
    url = "/archives/"
    weight = 2
    [menu.main.params]
        icon = "archives"

[[menu.main]]
    identifier = "categories"    # <--- Added Categories Menu Item
    name = "Categories"
    url = "/categories/"
    weight = 3
    [menu.main.params]
        icon = "categories"           # Standard icon for lists

[[menu.main]]
    identifier = "search"
    name = "Search"
    url = "/search/"
    weight = 4
    [menu.main.params]
        icon = "search"